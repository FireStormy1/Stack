# STACK music

The first build uses a quiet synthesized ambient bed so the game stays playable
without bundled audio files. If you replace it with recorded tracks later, use
loopable `.mp3` files named `music1.mp3` through `music10.mp3` in this folder.
